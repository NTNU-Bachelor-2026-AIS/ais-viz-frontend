import { render, screen } from '@testing-library/react'
//import Button from '../../src/components/Button'

// Since its using react components it is a tsx file. I am also using a fake component for now just to set up a basic template to follow in the future
describe('Button', () => {
    it('should render a button with the name that is provided', () => {
       //render(<Button name="Push!" />)

       // Testing if it works
       //screen.debug

       // Example of testing the button when its added.
       //const button = screen.getByRole('button');
       //expect(button).toBeInTheDocument();
       //expect(button).toHaveTextContent(/push/i)
    })
})